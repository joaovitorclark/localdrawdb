# Dados do usuário — NUNCA versionar (input, output, project.dbml, canvas.json)
/data/

# Artefatos proprietários / acoplados a domínio local (nunca versionar)
project.dbml
!examples/demo_lakehouse_oracle/project.dbml
examples/input/autorizacao*.sql
scripts/generate-lineage-fields.ts

# SQLs locais / fixtures temporários (ex.: exports de domínio)
tmp/

# Dependências e builds
node_modules/
dist/
dist-win/
.cache/
*.log
npm-debug.log*

# Arquivos de amostra grandes / proprietários
*.erwin
*.xlsx

# Cache local de plugins do Claude Code (nunca versionar)
.claude/

# Rascunho de execução de planos (ledger, briefs, pacotes de review)
.superpowers/

# Sistema
.DS_Store
.env
.env.local
.localdrawdb-dev.json

# Backlog local (planejamento privado — não vai pro remoto)
spec/backlog/
